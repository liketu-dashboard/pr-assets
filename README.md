# pr-assets

Screenshot assets hot-linked by liketu PR descriptions via raw.githubusercontent.com URLs.

**Do not delete files or rewrite history** — open PR bodies reference them directly.
Layout: one directory per PR branch name.
